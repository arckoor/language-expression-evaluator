/* eslint-disable no-unused-vars */
export enum Users {
	SELF,
	USER
}

export enum Replies {
	DEFAULT = "default",
	SUGGESTION = "suggestion",
	UNKNOWN = "unknown"
}
/* eslint-enable no-unused-vars */
